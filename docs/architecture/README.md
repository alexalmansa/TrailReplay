# Architecture Notes

This folder holds the durable architecture references for TrailReplay.

- `store.md`: Zustand store shape, slice ownership, and mutation boundaries.

Use ADRs in `docs/adr` when a decision affects multiple parts of the app or
changes an existing architectural rule.
